import 'package:flutter/material.dart';

class MessageToPersonScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Mesajlar')),
      body: Center(child: Text('Mesajlar Ekranı')),
    );
  }
}
